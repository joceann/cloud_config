eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761//eureka/  #euraka server addr

spring:
  application:
    name: cloud-feign
server:
  port: 8000

feign:
  hystrix:
    enabled: true
